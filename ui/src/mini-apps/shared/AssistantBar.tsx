// AssistantBar removed — replaced by chart info panels in each app.
// Kept as empty export so build doesn't break during migration.
export function AssistantBar(_props: { contextHint: string; onSend: (text: string) => Promise<unknown> }) {
  return null;
}
