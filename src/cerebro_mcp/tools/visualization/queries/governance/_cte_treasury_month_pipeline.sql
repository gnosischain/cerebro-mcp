-- Month-end pipeline: candidate days, per-token served day, two-step positions,
-- month base, registry, hub, enrichment and classification — in dependency order.
@candidates,
@served,
@positions,
@registry,
@hub,
@enrich,
@classified
