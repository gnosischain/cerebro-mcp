-- As-of pipeline: served-date resolution, supply, two-step positions, base rows,
-- registry, hub, enrichment and classification — in dependency order.
@asof,
@supply,
@positions,
@base,
@registry,
@hub,
@enrich,
@classified
